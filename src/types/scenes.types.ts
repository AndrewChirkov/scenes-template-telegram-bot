import { CustomContext } from "./custom-context.types"
import { User } from "./user.types"

export const enum Scenes {
  Start = "START",
  SelectLanguage = "REGISTER@SELECT_LANGUAGE",
  RegisterNickname = "REGISTER@SET_NICKNAME",
}

